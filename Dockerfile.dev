# Use a full Node image for dev (with native build toolchain if needed)
FROM node:20

WORKDIR /app

# Install deps first for better caching
COPY package*.json ./
RUN npm install --legacy-peer-deps

# Don't copy source here—Compose will bind mount it
EXPOSE 3000
